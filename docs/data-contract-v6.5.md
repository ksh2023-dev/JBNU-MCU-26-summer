# 데이터 계약 문서 (프론트엔드 ↔ 백엔드)

연수매칭 · 의대 연구 추천 시스템
수정: 김승하 · **v6.5** (2026-08-18 팀 논의 결정 반영)

> ## 📌 이 문서(v6.5)가 현재 최신 기준 문서입니다
>
> 프론트엔드·백엔드 구현의 기준(source of truth)은 **v6.5**입니다.
> `data-contract-v6.4.md` 이하 파일은 **이력 보존용**이며, 내용이 v6.5와 다르면 **v6.5가 우선**합니다.
>
> | 버전 | 파일 | 상태 |
> | --- | --- | --- |
> | **v6.5** | `docs/data-contract-v6.5.md` | **현재 기준** |
> | v6.4 | `docs/data-contract-v6.4.md` | 이력 (직전 버전) |
> | v6.3 · v6.2 · v6.1 · v6 | `docs/data-contract-v6.3.md` 외 | 이력 |

---

## 변경 이력

### v6.4 → v6.5 (이번 개정 — 현재 문서)

2026-08-18 팀 논의 결정을 반영한 개정입니다.

- **API 엔드포인트 변경 없음** (경로·메서드 그대로)
- **요청 구조 변경 없음** (`query` · `filters` · `sort` · `minScore` · `page` 그대로)
- **교수 상세 응답의 `papers` 내부 논문 식별자 구조는 변경됨** — `pmid` / `kciId` 두 필드 → **`articleId` 단일 필드**
- **응답 `keywords`는 필드 이름·타입 그대로**이고, 그 배열을 **무엇으로 채우는지**(MeSH 우선 → KCI fallback → `[]`)가 확정됩니다
- 그 외에는 프론트의 화면 표시 정책·링크 조립 규칙, 그리고 백엔드/데이터 파이프라인 내부의 키워드 원본 보존(`meshTerms` / `kciKeywords`) · `latestPaper` 산정 범위와 내부 스키마 · 논문 귀속 규칙이 바뀝니다

| # | 변경 | 영향 |
| --- | --- | --- |
| 1 | **`keywords` 화면 노출 정책 확정** — 교수 카드에서는 앞 **4개**까지 + 나머지 `+N`, 교수 상세에서는 **전체** 표시. v6.4의 "화면 미표시"를 대체한다 | 1-1, 1-2, 4장 |
| 2 | **`keywords` 데이터 선택 정책 확정** — 최종 `keywords`는 **MeSH 우선 → MeSH가 없으면 KCI keywords → 둘 다 없으면 `[]`**. 원본은 `meshTerms` / `kciKeywords`로 **출처별 분리 보존**(둘 다 내부 필드 · 응답에 추가하지 않음)하고, KCI keywords에 **별도 번역 과정을 두지 않는다**. v6.4의 "MeSH 기반 영문 키워드"를 대체한다 | 1-1, 5장 |
| 3 | **논문 PubMed · KCI 링크 조립 규칙 확정** — KCI URL 형식 확정. **링크 문자열은 데이터·응답에 저장하지 않고** 프론트가 `articleId`로 조립한다. 양쪽에 있는 논문은 **PMID 우선**이라 PubMed 링크가 된다 | 1-2, 3장 |
| 4 | **`latestPaper` 산정 범위 확정** — featured 선정용 내부 필드 `latestPaper`는 **PMID가 있는 PubMed 논문만** 대상으로 산정한다. KCI-only 논문(원본 PMID 없음 · 응답 `articleId`가 `ART...`)은 산정에서 제외되며, PubMed 논문이 없는 교수는 featured 후보에서 제외된다. 대표 논문 `papers`의 포함 범위는 종전대로 유지된다 | 2장 API ③ |
| 5 | **`latestPaper` 내부 스키마 정의** — `pmid` + `publishedAt`을 담은 object, PubMed 논문이 없으면 `null`. featured 정렬 기준은 `latestPaper.publishedAt`. **내부 필드이므로 API ③ 응답에는 포함하지 않는다** | 2장 API ③ |
| 6 | **동일 논문의 복수 교수 귀속 규칙 확정** — 검증된 공저 논문은 각 교수에게 모두 귀속한다. 서로 다른 교수 레코드 간 동일 `articleId`를 허용하고(교수 간 중복 제거 금지), **동일 교수의 `papers` 내부에서는 중복 저장을 금지**한다. 교수 귀속에는 **실제 저자임을 확인하는 절차가 필수**다 | 1-2 |
| 7 | **논문 식별자 응답 구조 통합** — API 응답의 `papers[].pmid` · `papers[].kciId` 두 필드를 **`papers[].articleId` 단일 필드(string · 필수)** 로 통합한다. `"ART"`로 시작하면 KCI, 아니면 PMID. 내부 파이프라인은 원본 PMID·KCI ID를 계속 보존할 수 있으나 **응답에는 하나만 나간다**. v6.4의 "식별자 이중화"를 대체한다 | 원칙 1, 1-2, 3장, 4장 |

> 이 표는 요약입니다. 각 항목의 상세 규칙은 "영향" 열이 가리키는 본문 절을 따릅니다.
> 4·5·6번은 백엔드·데이터 파이프라인 내부 규칙이며 그 자체로 응답 스키마를 바꾸지 않습니다 — `latestPaper`는 종전과 같이 응답에 포함되지 않습니다.
> 2번도 **응답 필드가 늘어나지 않습니다** — `meshTerms`·`kciKeywords`는 내부 원본 보존용이고, 응답에 나가는 키워드 필드는 종전대로 최종 `keywords` 하나뿐입니다. 다만 그 배열을 **무엇으로 채우는지**가 바뀌므로 데이터 파이프라인 수정이 필요합니다.
> **응답 스키마가 바뀌는 것은 7번뿐입니다** (`papers[].pmid` · `papers[].kciId` → `papers[].articleId`). 이 항목은 **프론트·백엔드 양쪽 수정이 필요합니다.**
> `keywords` 화면 노출·링크 조립(1·3번)만 놓고 보면 응답 스키마 변경이 없어 백엔드 수정이 필요하지 않지만, **7번 논문 식별자 통합에는 백엔드 응답 수정이 필요합니다.**

### v6.3 → v6.4

2026-08-16 회의 결정을 반영한 개정입니다. **응답 스키마가 바뀌는 항목이 있어 프론트·백엔드 양쪽 수정이 필요했습니다.**

| # | 변경 | 영향 |
| --- | --- | --- |
| 1 | **교수 DB 대상 범위 확정** — 의대 공식 명단 기준 (치과 계열·병원 전용 교수 제외) | 신설 0-2장 |
| 2 | **논문 식별자 이중화** — `pmid` 또는 `kciId` 필수 (국내 학술지 KCI 보완) | 원칙 1, 1-2, 3장 |
| 3 | **PubMed·KCI 중복 논문 처리 규칙** 신설 | 1-2 |
| 4 | **`labName` 필드 삭제** — 수집 가능한 출처가 없어 전원 `null`, 화면에서도 제거 | 1-2, 4장 |
| 5 | **`keywords` 화면 미표시** — 값은 영문(MeSH)이며 검색 매칭 전용으로 사용 | 1-1, 1-2 |
| 6 | **검색어 별칭(한↔영·학과/진료과) 규칙** 예정 항목으로 명시 | 5장 |
| 7 | 데이터 갱신·배포 구성 기록 (Vercel + 고정 IP 서버) | 7장 신설 |

> v6.4의 5번(`keywords` 화면 미표시)은 **v6.5에서 대체되었습니다.** 현재 기준은 아래 1-1의 화면 표시 정책입니다.
> v6.4 표의 "값은 영문(MeSH)"이라는 서술도 **당시 계약의 이력 기록**이며 현재 계약이 아닙니다. **v6.5의 현재 규칙은 MeSH 우선 → 없으면 KCI keywords → 둘 다 없으면 `[]`** 입니다 (1-1).
> v6.4에서 미확정으로 남겨 두었던 **KCI URL 형식**도 v6.5에서 확정되었습니다 (3장).
> v6.4의 2번(**논문 식별자 이중화 — `pmid` / `kciId` 두 필드**)은 **v6.5에서 `articleId` 단일 필드로 대체되었습니다** (원칙 1 · 1-2 · 3장). 위 v6.4 표의 서술은 **당시 계약의 이력 기록**이며, 현재 응답 계약이 아닙니다.

### 그 이전

- v6.2 → v6.3: 찜 목록 화면의 교수 데이터 조회를 API ① 재사용으로 확정 (요청/응답 필드 변경 없음)
- v6.1 → v6.2: API ① 경로·메서드 명시, `matchScore` null 허용, browse 정책, 검색 대상 필드 확정, featured 선정 기준 구체화
- v6 → v6.1: 필터 초기화 시 검색어 유지 명확화
- v5 → v6: `getPopularProfessors` → `getFeaturedProfessors`, "우수 교수" → "최근 연구 활동 교수"

---

> 읽는 법: "어떤 버튼을 누르면 → 프론트가 무엇을 요청하고 → 백엔드가 무슨 모양으로 응답하는지"의 약속입니다.
> 언어(프론트=JavaScript, 백엔드=Python)는 서로 달라도 되지만, 주고받는 데이터의 모양(=칸 이름)은 이 문서로 똑같이 맞춥니다.

---

## 0. 대전제

### 0-1. 할루시네이션 방지 4원칙

이 프로젝트는 없는 값을 지어내는 순간 서비스가 실패합니다. 아래 4원칙은 백엔드가 응답을 만들 때, 그리고 AI로 데이터를 수집·정리·생성할 때 반드시 지킵니다.

1. **모든 API 응답 논문에는 `articleId`가 필수.** `articleId`가 없는 논문은 응답에 넣지 않는다. `articleId`는 **유효한 PMID 또는 KCI article ID**여야 하며, 형태만 그럴듯한 값을 지어내지 않는다. *(v6.5 개정 — v6.4의 "`pmid` 또는 `kciId` 필수"를 단일 필드로 통합)*
   - **내부 데이터 파이프라인은 원본 PMID·KCI ID를 함께 보존할 수 있다.** 이는 내부 데이터의 이야기이고, **외부 API 응답에 나가는 것은 `articleId` 하나뿐이다** (1-2)
2. 값이 없는 필드는 지어내지 말고 `null`로 보낸다. (화면 표시 문구 "정보 없음"은 프론트가 붙인다. 빈칸으로 두지 않는다)
3. **검색어가 있는 경우**, `matchScore`가 임계값(`minScore`) 미만인 교수는 결과에서 제외한다. 억지로 채우지 않는다.
   - 빈 검색어(`query: ""`) 전체 조회에서는 `matchScore`가 `null`이라 임계값 비교가 불가능하므로 `minScore`를 적용하지 않는다. (2장 API ① browse 정책)
4. 응답에 데이터 수집 기준일(`collectedAt`)을 담는다. 교수명·이메일·논문 값은 DB 원본 그대로, 재작성 금지.

AI로 데이터를 만들 때 프롬프트 맨 앞에 붙일 규칙 (그대로 복사):

```
너는 <교수_데이터> 안의 값만 사용한다.
- 데이터에 없는 교수·논문·연구실·이메일·ORCID를 절대 생성하지 마라.
- 값이 없으면 지어내지 말고 null로 두어라.
- 논문은 articleId가 있는 것만 포함하라. articleId는 원본 PMID 또는 KCI article ID(ART로 시작) 값을 그대로 쓰고, 새로 만들어내지 마라.
- 제목·학술지·연도는 원본 그대로 두어라.
- 근거가 부족하면 "제공된 데이터로는 확인할 수 없습니다"라고만 답하라.
```

### 0-2. 교수 DB 대상 범위 (v6.4 신설 · 2026-08-16 회의 확정)

**전북대학교 의과대학 공식 명단(교실/교수 페이지)에 게시된 교수를 대상으로 한다.**

| 구분 | 처리 | 사유 |
| --- | --- | --- |
| 의대 명단 게시 교수 | **포함** | 기준 명단 |
| 치과 계열 진료과 소속 | **제외** | 의과대학 소속이 아님 |
| 병원 명단에만 있고 의대 명단에 없는 교수 | **제외** | 의대 공식 명단 기준에 해당하지 않음 |

- 현재 수록 인원: **182명** (임상의학 145 · 기초의학 35 · 의학교육학 1 · 인문사회의학 1)
- 제외된 교수의 데이터는 삭제하지 않고 파이프라인 산출물에 보존한다. 범위가 변경되면 재조립으로 복구된다
- 범위 변경은 계약 개정 사항이다. 임의로 바꾸지 않는다

---

## 1. 공통 데이터 객체

### 1-1. 교수 카드 (검색 결과 · featured 1건)

```json
{
  "id": "P-012",
  "name": "김민수",
  "profileImageUrl": null,
  "professorType": "임상의학",
  "department": "순환기내과",
  "specialties": ["심장영상", "심부전"],
  "keywords": ["cardiac imaging", "heart failure", "biomarker"],
  "matchScore": 0.87
}
```

- 같은 모양을 교수 검색 결과와 featured(최근 연구 활동 교수)에서 공통으로 사용
- `id` — 내부 식별자, 화면에는 표시하지 않음. **한 번 부여되면 변경되지 않는다** (프론트 찜이 localStorage에 id로 저장되므로)
- `profileImageUrl` — 없으면 `null` (프론트가 이니셜 아바타로 대체)
- `professorType` — 교수 구분 필터값. 값 집합: `기초의학 | 임상의학 | 의학교육학 | 인문사회의학`
- `department` — 소속교실 또는 진료과. **분과·겸직이 병기될 수 있다** (예: `내과학교실(소화기)`, `예방의학교실 · 가정의학교실`)
- `specialties` — 전문 분야. 여러 개를 담는 배열(`[ ]`). 값이 하나여도 배열로 보냄. 없으면 빈 배열 `[]`
- `keywords` — **응답에는 전체 배열을 담고, 카드 화면에는 앞 4개까지 표시한다 (v6.5)**. 아래 참고
- `matchScore` — **`number | null`** (아래 참고)
- 찜 여부·이메일은 카드에 넣지 않음 — 찜 여부는 프론트가 `id`를 localStorage와 대조해 판단, 이메일은 상세에서만 표시

#### `keywords` — 데이터 선택 규칙 + 화면 표시 정책 (v6.5 확정)

**데이터 선택 규칙 — v6.5 확정 (v6.4의 "MeSH 기반 영문 키워드"를 대체)**

`keywords`는 **서비스 검색과 화면 표시에 사용하는 최종 키워드 배열**이며, 아래 우선순위로 만들어진다.

| 순위 | 조건 | 결과 |
| --- | --- | --- |
| 1 | `meshTerms`가 존재하고 **비어 있지 않음** | `keywords = meshTerms` |
| 2 | `meshTerms`가 없거나 `[]`이고 **`kciKeywords`가 존재함** | `keywords = kciKeywords` |
| 3 | **둘 다 없음** | `keywords = []` |

```text
MeSH 있음                    → MeSH 사용
MeSH 없음 + KCI keyword 있음 → KCI keyword 사용
둘 다 없음                   → keywords: []
```

- **백엔드는 최종 `keywords` 전체 배열을 응답에 담는다.** 개수를 잘라서 보내지 않는다
- **검색 매칭에는 전체 `keywords`를 사용한다.** 화면에 몇 개를 보여주는지와 무관하다
- **`keywords`의 값이 항상 영문이라고 전제하지 않는다.** MeSH를 쓰는 경우에는 영문 MeSH 용어이고(예: `"cardiac imaging"`), MeSH가 없어 KCI keyword를 쓰는 경우에는 **KCI가 제공한 원본 그대로**다
- 한글 검색어와 영문 값 사이의 간극은 5장의 **검색어 별칭 규칙**으로 해결한다 — `keywords` 자체를 바꾸는 것이 아니라, 검색어 쪽에서 별칭을 붙여 매칭한다

**원본 키워드는 출처별로 분리 보존한다 — `meshTerms` / `kciKeywords` (v6.5 확정)**

내부 데이터 예:

```json
{
  "meshTerms": ["Heart Failure", "Cardiac Imaging"],
  "kciKeywords": ["심부전", "Heart Failure"]
}
```

| 필드 | 위치 | 역할 |
| --- | --- | --- |
| `meshTerms` | **내부 데이터 전용** | PubMed/MeSH에서 수집한 **원본** 키워드를 그대로 보존 |
| `kciKeywords` | **내부 데이터 전용** | KCI에서 수집한 **원본** 키워드를 그대로 보존 |
| `keywords` | **API 응답 필드** | 위 우선순위(MeSH 우선 → KCI fallback → `[]`)로 만든 **최종** 배열. 검색·화면 표시에 사용 |

- `meshTerms`·`kciKeywords`는 **원본 보존용 내부 필드이며, 현재 API 응답에 새 필드로 추가하지 않는다.** 응답에 나가는 키워드 필드는 종전대로 최종 `keywords` 하나뿐이다

**KCI keywords 번역 정책 (v6.5 확정)**

- KCI는 한글·영문 keywords를 함께 제공하므로 **서비스에서 별도의 AI·KMLE 번역 과정을 두지 않는다**
- KCI에서 받은 keyword 원본은 `kciKeywords`에 보존한다
- MeSH가 없는 경우 **KCI가 제공한 keyword를 별도 번역 없이 fallback으로 활용한다**
- **프론트는 keyword를 번역하거나 새로 생성하지 않는다** (원칙 2). 받은 `keywords` 배열을 그대로 표시한다
- KCI가 제공하는 한글·영문 중 **어느 쪽을 최종 `keywords`에 우선 넣을지는 이 계약에서 규정하지 않는다.** 현재 계약은 "KCI가 제공한 keyword를 별도 번역 없이 활용한다"까지이며, "영문 우선"·"한글 우선" 같은 언어 우선순위 규칙은 두지 않는다

**화면 표시 정책 — v6.5 신설 (v6.4의 "화면 미표시"를 대체)**

| 화면 | 표시 |
| --- | --- |
| 교수 카드 — HomePage · SearchResultPage · FavoritesPage | 응답 배열의 **순서를 유지한 채 앞 4개까지** |
| 교수 카드에서 4개를 초과할 때 | 나머지 개수를 **`+N`** 으로 표시 (예: 총 7개 → 앞 4개 + `+3`) |
| 교수 상세 — ProfessorDetailPage | **전체 표시** (자르지 않는다) |
| `keywords: []` | 카드·상세 **모두 해당 키워드 영역을 표시하지 않는다** |

- 위 세 화면은 같은 `ProfessorCard` 컴포넌트를 쓴다. **표시 개수 제한은 `ProfessorCard` 한 곳에서만 한다** — 페이지가 미리 잘라서 넘기면 같은 교수 데이터가 화면마다 다르게 보이고, `+N`도 이미 잘린 배열 길이로 계산되어 실제 개수와 어긋난다
- **`keywords` 배열의 순서는 백엔드가 응답으로 제공한 순서를 그대로 사용한다.** 프론트는 중요도·관련도를 재계산하거나 배열을 재정렬하지 않고, 그 순서 위에서 "표시 개수"만 앞에서 자른다
- 백엔드 내부의 `keywords` **배열 정렬 방식**은 **프론트 화면 표시 계약의 범위 밖**이다. 프론트 구현을 위해 그 결정을 기다릴 필요는 없다. (**어느 출처에서 값을 가져올지**는 위 데이터 선택 규칙으로 이미 확정되었다 — 범위 밖인 것은 그렇게 만들어진 배열의 **순서**뿐이다)

> 카드 앞 4개 + `+N`, 상세 전체 표시라는 v6.5 `keywords` 화면 정책은 이 내용으로 완결된다.

#### `matchScore` — `number | null` (v6.2 확정, 변경 없음)

| 상황 | 값 |
| --- | --- |
| 검색어가 있는 검색 결과 (`query`에 값 있음) | `0 ~ 1` 사이 실수 (관련도 점수) |
| 빈 검색어 전체 조회 (`query: ""`) | `null` |
| featured 교수 카드 (API ③) | `null` |

- **`matchScore: null`은 오류가 아니라 정상 상태다.** 검색어가 없어 관련도를 계산하지 않았다는 뜻이다 (원칙 2·3)
- 프론트는 `matchScore`가 항상 숫자라고 가정하지 않는다. 현재 카드 UI는 점수를 표시하지 않는다

### 1-2. 교수 상세

```json
{
  "id": "P-012",
  "name": "김민수",
  "profileImageUrl": null,
  "professorType": "임상의학",
  "department": "순환기내과",
  "specialties": ["심장영상", "심부전"],
  "keywords": ["cardiac imaging", "heart failure", "biomarker"],
  "email": "kim@jbnu.ac.kr",
  "homepageUrl": null,
  "papers": [
    { "title": "Prognostic value of cardiac MRI ...", "journal": "JACC", "year": 2022,
      "articleId": "35123456" },
    { "title": "국내 심부전 환자의 예후 인자 분석", "journal": "대한내과학회지", "year": 2021,
      "articleId": "ART002712345" }
  ]
}
```

- 위쪽 `id` ~ `keywords`는 카드(1-1)와 동일한 칸 이름을 사용
- `keywords` — 응답은 카드와 같은 **최종 전체 배열**(MeSH 우선 → 없으면 KCI → 둘 다 없으면 `[]` · 1-1)이고, 상세 화면에서는 **자르지 않고 전체를 표시한다 (v6.5)**. 빈 배열 `[]`이면 키워드 영역 자체를 표시하지 않는다
- **`labName` 필드는 v6.4에서 삭제되었다.** 수집 가능한 출처가 없어 전원 `null`이었고, 프론트 화면에서도 제거하기로 확정(2026-08-16 회의)
- `homepageUrl` · `email` — 데이터 없으면 `null`
- `homepageUrl` — 임상 교수는 병원 프로필, 기초 교수는 학교 홈페이지 URL
- `papers` — 최신순 1편 + 인용수 상위 2편 = 3편 (7/23 회의 결정). 논문이 없으면 빈 배열 `[]`

#### `papers`의 논문 식별자 `articleId` (v6.5 확정 — `pmid` / `kciId` 통합)

**API 응답의 논문 식별자는 `articleId` 단일 필드다.** v6.4의 `pmid` / `kciId` 두 필드는 **v6.5에서 이 필드로 통합되었고, 응답에서 제공하지 않는다.**

| 필드 | 타입 | 필수 | 의미 |
| --- | --- | --- | --- |
| `articleId` | `string` | **필수 (모든 응답 논문)** | 논문 식별자. 접두사로 출처를 구분한다 |

**출처 판별 — `"ART"` 접두사**

| `articleId` | 출처 | 예 |
| --- | --- | --- |
| `"ART"`로 **시작함** | **KCI** 식별자 | `"ART002712345"` |
| `"ART"`로 **시작하지 않음** | **PMID** (PubMed) | `"35123456"` |

- **백엔드는 `"ART"`로 시작하지 않는 `articleId`가 유효한 PMID임을 보장한다.** 프론트는 이 보장을 전제로 링크를 조립한다 (3장)
- `articleId`는 **모든 응답 논문에 반드시 있다.** 식별자가 없는 논문은 응답에 넣지 않는다 (원칙 1)
- **`articleId` 자체는 화면에 표시하지 않는다. 원문 링크 생성에만 사용한다** (3장 참고)
- 논문 식별자 이름으로 `articleId`를 쓰는 이유는 **교수 객체의 `id`와 구분하기 위해서다.** 논문에는 `id`를 쓰지 않는다
- **논문 URL 문자열은 데이터·API 응답에 저장하지 않는다 (v6.5).** `papers`에는 `articleId`만 담고, 주소는 프론트가 조립한다 (3장)

예 — PubMed 논문

```json
{
  "title": "Example Paper",
  "journal": "Example Journal",
  "year": 2025,
  "articleId": "35123456"
}
```

예 — KCI-only 논문

```json
{
  "title": "국내 연구 예시",
  "journal": "대한OO학회지",
  "year": 2025,
  "articleId": "ART002712345"
}
```

**PubMed·KCI 양쪽에 존재하는 동일 논문 — 내부 데이터와 API 응답을 구분한다**

| 구분 | 식별자 |
| --- | --- |
| 데이터 수집·파이프라인 **내부** | 필요하면 **원본 PMID와 KCI ID를 모두 보존할 수 있다** |
| **API 응답** | 논문당 **`articleId` 하나만** 제공한다 |

- **PMID가 있으면 PMID를 우선하여 `articleId`로 사용한다** (PubMed + KCI 모두 존재 → `articleId = PMID`)
- **PMID가 없고 KCI ID만 있으면 KCI ID(`ART...`)를 `articleId`로 사용한다** (KCI-only → `articleId = ART...`)
- 즉 내부에 두 원본 식별자가 보존되어 있어도, 응답에서는 위 우선순위로 **하나만** 선택해 내보낸다

#### PubMed · KCI 중복 논문 처리 (v6.4 신설 · 적용 범위는 v6.5에서 명시)

일부 국내 학술지(J Korean Med Sci, Korean J Intern Med 등)는 PubMed에도 등재되어 있어 같은 논문이 양쪽에서 수집될 수 있다. 파이프라인은 아래 규칙으로 하나의 논문으로 합친다.

> **이 절은 `동일한 교수`에 대해 같은 논문이 두 출처(PubMed · KCI)에서 수집된 경우의 병합 규칙이다.** 서로 다른 교수 레코드 사이의 동일 논문은 병합 대상이 아니며, 바로 아래 `동일 논문의 복수 교수 귀속`을 따른다 (v6.5).

**동일 논문 판별 (우선순위 순)**

1. **DOI 일치** — 양쪽 모두 DOI를 제공하므로 최우선 기준
2. DOI가 없으면 **정규화한 제목 + 발행연도** 일치 (대소문자·공백·구두점 무시, 영문 제목 우선 비교)
3. 그래도 판별이 어려우면 **별개 논문으로 두고 검수 목록에 기록한다.** 임의로 합치지 않는다 (원칙 2)

**병합 정책**

- 중복 판정 시 레코드를 하나로 합치고, **내부 데이터에는 원본 PMID와 KCI ID를 모두 보존**한다
- **API 응답에는 `articleId` 하나만 나간다 (v6.5).** 병합된 논문은 **PMID를 우선**해 `articleId`로 사용하고, PMID가 없으면 KCI ID(`ART...`)를 사용한다 (1-2 식별자 설명)
- 대표 논문 3편 선정의 인용수 기준: **국제 인용수(OpenAlex) 우선, 없으면 KCI 피인용수**
- 초록은 영문(PubMed)과 한글(KCI)을 내부 데이터로 함께 보존한다 (검색 매칭에 활용)
- **키워드도 출처별로 분리 보존한다 (v6.5)** — PubMed/MeSH 원본은 `meshTerms`, KCI 원본은 `kciKeywords`에 담고, 최종 `keywords`는 **MeSH 우선 → 없으면 KCI** 규칙으로 만든다 (1-1). 두 원본 필드는 내부 데이터 전용이며 응답에 나가지 않는다

#### 동일 논문의 복수 교수 귀속 (v6.5 신설)

동일한 논문에 **본 서비스 대상 교수가 여러 명 공저자로 참여한 경우, 해당 논문은 각 교수의 논문으로 모두 인정한다.**

바로 위의 중복 병합 규칙과 적용 범위가 다르므로, 아래 두 가지를 반드시 구분한다.

| 구분 | 상황 | 처리 |
| --- | --- | --- |
| **A. 동일 교수 내부 중복** | 같은 교수의 같은 논문이 PubMed와 KCI에서 각각 수집됨 | **하나로 병합한다.** 위 `PubMed · KCI 중복 논문 처리`의 판정 규칙(DOI → 정규화 제목+연도 → 검수)을 그대로 적용하고, 병합 후 내부 데이터에는 원본 PMID·KCI ID를 모두 보존한다. 응답에는 `articleId` 하나만 나간다. **동일 교수의 `papers` 배열 안에 같은 논문을 두 번 저장하지 않는다** |
| **B. 서로 다른 교수 간 동일 논문** | 교수 A와 교수 B가 같은 논문의 공저자 | **각 교수에게 모두 귀속한다.** 교수 레코드 간에는 **동일한 `articleId`가 존재할 수 있으며**, **교수 간 중복 제거를 수행하지 않는다** |

**B — 교수 간 동일 식별자 허용**

- 서로 다른 교수 레코드 간 **동일한 `articleId`의 중복을 허용한다**
- 동일한 논문이 교수 A와 교수 B의 `papers`에 각각 존재할 수 있다
- 이는 **데이터 중복 오류가 아니라 정상적인 공저자 관계**다
- 교수 간 **전역(global) 기준으로 `articleId`를 unique하게 만들거나, 하나의 교수에게만 논문을 귀속해서는 안 된다**

예 — 아래 구조는 정상적인 데이터다.

```json
// 교수 A
{
  "papers": [
    { "title": "Example Paper", "articleId": "12345678" }
  ]
}

// 교수 B
{
  "papers": [
    { "title": "Example Paper", "articleId": "12345678" }
  ]
}
```

**교수 귀속 시 저자 검증 필수**

- 논문을 교수에게 귀속할 때에는 **해당 교수가 실제 그 논문의 저자임이 검증되어야 한다**
- **동일한 `articleId`가 다른 교수 레코드에 이미 존재한다는 사실만으로 해당 논문을 새로운 교수에게 귀속해서는 안 된다**
- 교수별 논문 귀속은 **해당 교수가 실제 저자임이 확인된 경우에만** 인정한다
- 저자임을 어떤 방법으로 확인할지(검증 절차)는 **아직 별도 문서로 확정되지 않았다.** 이 계약은 특정 검증 절차가 이미 존재한다고 전제하지 않으며, 절차가 확정되면 그 문서를 따른다
- 즉 위 `동일 식별자 중복 허용`은 **검증된 공저자 관계를 표현하기 위한 규칙**이지, 동일한 `articleId`를 근거로 **논문을 다른 교수에게 자동 복제하는 규칙이 아니다**
- 저자임을 확인할 근거가 부족하면 **임의로 해당 교수의 논문으로 추가하지 않는다** (원칙 2)

---

## 2. API 엔드포인트

프론트의 `api/professorApi.js` 함수와 1:1로 대응합니다.

**v6.5에서 바뀐 것과 바뀌지 않은 것**

- **엔드포인트 변경 없음** — 경로·메서드 그대로
- **요청 구조 변경 없음** — `query` · `filters` · `sort` · `minScore` · `page` · `pageSize` 그대로
- **교수 상세(API ②) 응답의 `papers` 식별자 구조는 변경됨** — `pmid` / `kciId` 두 필드 → **`articleId` 단일 필드** (원칙 1 · 1-2). 응답에서 바뀌는 것은 이 항목뿐이며, 나머지 응답 필드는 그대로다
- `keywords`는 **필드 이름·타입 모두 그대로**다. 배열을 채우는 규칙(MeSH 우선 → KCI fallback → `[]`)만 v6.5에서 확정되었다 (1-1)
- `latestPaper`·`meshTerms`·`kciKeywords`는 **내부 필드이므로 어떤 응답에도 포함되지 않는다**

| 계약 | 경로 | 프론트 함수 |
| --- | --- | --- |
| API ① 교수 검색·목록 조회 | `POST /api/professors/search` | `getProfessors(query, filters)` |
| API ② 교수 상세 조회 | `GET /api/professors/{id}` | `getProfessorById(id)` |
| API ③ 최근 연구 활동 교수 조회 | `GET /api/professors/featured` | `getFeaturedProfessors()` |

### API ① 교수 검색·목록 조회

- 트리거: 검색 버튼 / Enter, 필터 선택 시 즉시 재조회, 페이지 번호 클릭

요청:

```json
{
  "query": "심장",
  "filters": {
    "professorType": ["임상의학", "기초의학"],
    "favoriteIds": ["P-001", "P-008", "P-013"]
  },
  "sort": "relevance",
  "minScore": 0.3,
  "page": 1,
  "pageSize": 5
}
```

#### 요청 필드 기본값·제약

| 필드 | 타입 | 기본값 | 비고 |
| --- | --- | --- | --- |
| `query` | string | `""` | 빈 문자열이면 전체 목록 조회(browse) |
| `filters.professorType` | string[] | `[]` | 빈 배열이면 전체. 허용값: `기초의학` / `임상의학` / `의학교육학` / `인문사회의학` |
| `filters.favoriteIds` | string[] \| null | `null` | `null`·없음 = 찜 필터 OFF, `[]` = 찜 필터 ON + 찜 0명 → 결과 0 |
| `sort` | string | `"relevance"` | MVP는 `relevance`만 지원 |
| `minScore` | number | `0.3` (**개발용 임시값**) | 0 ~ 1 |
| `page` | number | `1` | 1 이상 |
| `pageSize` | number | `5` | 한 페이지에 보여줄 교수 수 |

응답:

```json
{
  "results": [],
  "total": 12,
  "page": 1,
  "pageSize": 5,
  "collectedAt": "2026-08-16"
}
```

- `results` — 교수 카드(1-1) 배열
- `total` — 모든 필터를 적용한 뒤의 최종 개수(페이지를 자르기 전 값). 프론트가 이 값으로 페이지 수 계산
- `page` · `pageSize` — 요청 값을 그대로 반환
- `collectedAt` — 데이터 수집 기준일 (`"YYYY-MM-DD"`). 요청마다 바뀌는 값이 아니다 (원칙 4)

#### 검색 대상 필드

`query`는 아래 필드에서 **부분일치(대소문자 무시)** 로 검색한다.

- 교수명 (`name`) · `keywords` · `specialties` · 논문 제목 · **논문 초록** · 소속 (`department`)

> ⚠️ 논문 초록 검색은 v6.2에서 계약으로 확정한 사항이며, 백엔드 검색 로직 반영이 필요하다.
> ⚠️ 논문 제목·초록은 대체로 **영문**인데 검색어는 한글인 경우가 많다. `keywords`도 MeSH를 쓰는 교수는 영문이다 (MeSH가 없어 KCI keyword를 쓰는 경우에는 원본 그대로이며 한글일 수 있다 · 1-1). 이 간극은 5장의 **검색어 별칭 규칙**으로 해결한다.
> ⚠️ 검색 매칭에는 **전체 `keywords`**를 쓴다. 카드에 4개만 보인다고 해서 매칭 대상이 줄어드는 것이 아니다 (v6.5 · 1-1).

#### 정렬 규칙

```text
query가 있는 경우: matchScore 내림차순 → 동점이면 이름 오름차순
query가 없는 경우: 이름 오름차순
```

- 동점 시 이름 오름차순을 쓰는 이유 — 순서가 흔들리면 페이지를 넘길 때 결과가 중복되거나 누락된다

#### 빈 검색어(`query: ""`) — 전체 목록 조회(browse)

`query: ""`는 검색 실패가 아니라 **전체 교수 목록 조회**로 간주한다.

| 항목 | 동작 |
| --- | --- |
| `filters.professorType` · `filters.favoriteIds` | 그대로 적용 |
| `matchScore` | `null` — 근거가 없으므로 지어내지 않는다 (원칙 2) |
| `minScore` | **적용하지 않는다** |
| 정렬 | **이름 오름차순** |
| 페이지네이션 | 필터·정렬을 마친 뒤 `page` / `pageSize` 기준으로 자른다 |
| `total` | 필터 적용 후 전체 결과 수 |

#### `minScore`

- 이 값 미만의 `matchScore`를 가진 교수는 백엔드가 결과에서 제외한다 (원칙 3)
- **현재 개발용 임시 기본값 `0.3`** — 최종 threshold는 5장 참고
- 빈 `query`에서는 적용하지 않는다

#### "찜한 교수만 보기" 필터 (`filters.favoriteIds`)

프론트가 localStorage의 찜 id 목록을 실어 보내면, 백엔드가 교집합(AND)을 걸어 최종 결과를 만든 뒤 페이지네이션한다.

```
① localStorage에서 찜 id 읽기   → ["P-001", "P-008", "P-013"]
② 검색 조건과 함께 백엔드 전달   → query + filters(favoriteIds 포함)
③ 백엔드가 교집합 계산          → query ∩ professorType ∩ 찜 id
④ 5명씩 페이지네이션            → page / pageSize
⑤ results + total 반환          → 찜 필터까지 반영된 최종 수
```

- **왜 프론트에서 거르지 않는가** — 백엔드가 먼저 자른 페이지를 프론트가 다시 거르면 `total`과 어긋나 페이지 계산이 깨진다
- 세 가지 상태: `null`(필터 꺼짐) / `[]`(필터 켜짐·찜 0명 → 결과 0) / `[id...]`(교집합)

**존재하지 않는 교수 id가 섞인 경우**

- 없는 id는 교집합에서 **제외**된다. 백엔드는 없는 교수를 만들어 채우지 않는다 (원칙 2)
- `total`은 실제로 존재하는 교수 수를 반환한다. 화면 표시 수는 언제나 응답의 `total`을 따른다

> ⚠️ v6.4에서 교수 DB 대상 범위가 좁아졌으므로(0-2장), 기존 localStorage에 범위 밖 교수 id가 남아 있을 수 있다. 위 규칙에 따라 자동으로 제외되며 별도 처리는 필요 없다.

**찜 목록 화면(FavoritesPage)**은 별도 API 없이 API ①을 재사용한다 — `query: ""` + `filters.favoriteIds`(항상 배열). browse 정책이 그대로 적용된다.

### API ② 교수 상세 조회

- 트리거: 카드의 [자세히 보기] 클릭
- 요청: `GET /api/professors/{id}` (예: `/api/professors/P-012`)
- 응답: 교수 상세 객체(1-2)
- 없는 id: HTTP 404 + `{ "error": "not_found" }`

### API ③ 최근 연구 활동 교수 조회

- 트리거: 메인 페이지 진입
- 요청: `GET /api/professors/featured`
- 응답: `{ "results": [ 교수 카드 배열 ], "collectedAt": "..." }`

#### 선정 기준

- 교수별 **가장 최근 PubMed 논문의 발행일** 최신순 상위 **3명** *(v6.5 — 산정 범위를 PubMed 논문으로 확정)*
- 발행일 정보가 없는 교수는 후보에서 제외한다 (원칙 2)
- 선정은 백엔드가 수행한다. 프론트는 받은 결과를 그대로 표시한다 (`slice` 금지)
- 선정용 내부 필드(`latestPaper`)는 **응답에 포함되지 않는다** (스키마는 아래 참고)
- 유효 후보가 3명 미만이면 **있는 만큼만** 반환한다 (가짜로 채우지 않는다)

##### `latestPaper` 산정 범위 (v6.5 확정)

`latestPaper`는 **API 응답 필드가 아니라 백엔드·데이터 파이프라인 내부의 featured 선정용 필드**다 (종전과 동일). 이 필드를 무엇으로 산정하는지를 v6.5에서 다음과 같이 확정한다.

| 논문 | `latestPaper` 후보 |
| --- | --- |
| PMID가 있는 PubMed 논문 (응답 `articleId`가 `"ART"`로 시작하지 않는 논문) | **포함** |
| KCI-only 논문 (내부에 원본 PMID가 없는 논문 · 응답 `articleId`가 `"ART"`로 시작) | **제외** |

- `latestPaper` 후보에는 **PMID가 있는 PubMed 논문만 포함한다**
- 교수별 PubMed 논문 중 **가장 최근 논문**을 그 교수의 `latestPaper`로 사용한다
- **PubMed 논문이 없는 교수는 `latestPaper`를 산정할 수 없으므로 featured 후보에서 제외한다** (KCI-only 논문만 있는 교수 포함). 없는 값을 대체해 채우지 않는다 (원칙 2)
- `latestPaper`는 **내부 필드이므로 PubMed 논문 여부는 내부 파이프라인이 보존한 원본 PMID로 판단한다.** 응답 기준으로 말하면 `articleId`가 `"ART"`로 시작하지 않는 논문이 PubMed 논문이다 (1-2)
- **`articleId` 통합(v6.5)은 이 규칙을 바꾸지 않는다.** 식별자 표현이 한 필드로 합쳐졌을 뿐, `latestPaper`의 PubMed-only 산정 정책은 그대로다
- **KCI-only 논문을 산정에서 제외하므로, KCI와 PubMed의 발행일 정밀도 차이가 featured 선정에 영향을 주지 않는다** — 정렬에 쓰이는 값이 모두 PubMed 논문의 발행일 하나로 통일되기 때문이다

##### `latestPaper` 내부 스키마 (v6.5 확정)

교수에게 PubMed 논문이 **있는** 경우:

```json
"latestPaper": {
  "pmid": "35123456",
  "publishedAt": "2025-03-14"
}
```

교수에게 PubMed 논문이 **없는** 경우:

```json
"latestPaper": null
```

| 필드 | 타입 | 의미 |
| --- | --- | --- |
| `latestPaper` | `object \| null` | featured 선정을 위한 **내부 필드**. **API 응답에는 포함하지 않는다** |
| `latestPaper.pmid` | `string` | 해당 교수의 **가장 최근 PubMed 논문의 PMID** |
| `latestPaper.publishedAt` | `string` | 해당 PubMed 논문의 **발행일**. featured 정렬 기준 |

- **PubMed 논문이 있을 때만 `latestPaper` object를 만든다**
- **KCI-only 논문은 `latestPaper`가 될 수 없다** (위 산정 범위)
- **PubMed 논문이 없으면 `latestPaper = null`** 이고, 그 교수는 featured 후보에서 제외된다 (없는 값을 지어내지 않는다 · 원칙 2)
- **featured 정렬은 `latestPaper.publishedAt` 기준**이다
- `latestPaper`는 내부 필드이므로 **API ③ 응답에 포함하지 않는다**

> **왜 `latestPaper` 안에서는 `articleId`가 아니라 `pmid`인가** — `articleId`는 **API 응답의 `papers` 식별자**로 도입된 필드다 (1-2). `latestPaper`는 응답에 나가지 않는 **PubMed 전용 내부 필드**이고 후보가 PubMed 논문뿐이므로, 내부에서는 원본 `pmid`를 그대로 쓴다. 두 필드는 쓰이는 층이 다르며 서로 대체하지 않는다.

> **`publishedAt`의 값 포맷은 이 계약에서 확정하지 않는다.** 위 `"2025-03-14"`는 **예시 값**이며, 모든 값이 `YYYY-MM-DD`라는 뜻이 아니다 (PubMed는 연·월까지만 제공하는 논문이 있다). 포맷 확정이 필요하면 실제 파이프라인 산출물을 확인한 뒤 별도로 정한다 — 근거 없이 포맷을 단정하지 않는다 (원칙 2).

**`papers`의 범위와 혼동하지 않는다 — 두 범위는 서로 다르다**

| 구분 | 대상 범위 |
| --- | --- |
| 대표 논문 `papers` (1-2 · 응답 필드) | 기존 계약 그대로 — **PubMed 논문과 KCI-only 논문이 모두 포함될 수 있다** |
| featured 선정용 `latestPaper` (내부 필드) | **PubMed 논문만** |

- 즉 `latestPaper` 산정에서 KCI-only 논문을 제외한다는 것은 **featured 선정 기준에 한정된 규칙**이다. 교수 상세의 `papers`에서 KCI-only 논문을 빼라는 뜻이 **아니다** (3장 — `articleId`가 `"ART"`로 시작하는 국내 논문도 목록에서 제거하지 않는다)

### 찜하기 · 찜 취소 · 찜 목록

- **찜 id의 저장·추가·삭제** — 백엔드 API 없음. localStorage (`getFavorites` / `addFavorite` / `removeFavorite`)
- **찜한 교수의 데이터 조회** — API ① (localStorage에는 id만 있으므로 표시할 데이터는 백엔드에서 받는다)
- `isFavorite` · `favoriteDate`는 mock 테스트용이며 계약 응답에 넣지 않는다

---

## 3. 프론트엔드 내부 처리 (백엔드 요청 불필요)

- 찜하기 / 찜 취소 / 찜 **id 목록 읽기** — localStorage
- 필터 초기화 — 검색어는 유지하고 교수 구분·찜 필터만 기본값으로. 페이지는 1로
- 페이지네이션 — 응답의 `total`로 페이지 계산, 페이지 클릭 시 `page`만 바꿔 API ① 재호출
- 찜 여부 표시 — `id`를 localStorage와 대조
- **`keywords` 표시 개수 제한** — 카드는 앞 4개 + `+N`, 상세는 전체 (1-1). 응답을 자르지 않고 표시 단계에서만 제한한다
- **논문 원문 링크 조립** — 아래 참고

### 논문 원문 링크 (v6.5 확정)

**논문 URL 문자열은 데이터·API 응답에 저장하지 않는다.** 응답에는 식별자 `articleId`만 담고, 프론트가 아래 규칙으로 주소를 조립한다.

판별 기준은 **`articleId`의 `"ART"` 접두사 하나뿐이다 (v6.5).**

| 조건 | 조립할 URL | 버튼 문구 |
| --- | --- | --- |
| `articleId.startsWith("ART")` | `https://www.kci.go.kr/kciportal/ci/sereArticleSearch/ciSereArtiView.kci?sereArticleSearchBean.artiId={articleId}` | "KCI 보기" |
| 그 외 (PMID) | `https://pubmed.ncbi.nlm.nih.gov/{articleId}/` | "PubMed 보기" |

- **`"ART"`로 시작하지 않는 `articleId`는 유효한 PMID임을 백엔드가 보장한다** (1-2). 프론트는 별도 형식 검사 없이 PubMed 링크로 조립한다
- URL에 넣을 때는 **`encodeURIComponent(articleId)`** 로 감싼다
- **`articleId` 자체는 화면에 표시하지 않는다.** 링크 생성에만 쓴다
- **계약상 `articleId`가 없는 논문은 응답에 존재하지 않는다** (원칙 1). 그래도 프론트는 방어적으로 **`articleId`가 없으면 링크를 생성하지 않는다** — 주소를 지어내지 않기 위해서다 (원칙 2)
- **`articleId`가 `"ART"`로 시작하는 국내 논문도 목록에서 제거하지 않는다.** 접두사로 논문을 거르면 국내 학술지 논문이 통째로 사라진다
- PubMed·KCI 양쪽에 있는 논문은 백엔드가 **PMID를 우선**해 `articleId`로 내보내므로 (1-2), 프론트에는 자연히 PubMed 링크가 만들어진다 (영문 초록·인용 정보가 함께 보이므로)

---

## 4. 값 없음 처리 규칙 (원칙 2 · 빈칸 절대 금지)

| 필드 | 값이 없을 때 | 화면 표시 |
| --- | --- | --- |
| `profileImageUrl` | `null` | 이니셜 아바타 |
| `email` | `null` | "정보 없음" |
| `homepageUrl` | `null` | 링크 숨김 |
| `papers` | `[]` | "등록된 대표 논문 없음" |
| `papers[].articleId` | **응답 논문에는 반드시 있음** (없는 논문은 응답에 넣지 않음 — 원칙 1) | 표시하지 않음 (원문 링크 생성에만 사용) |
| `specialties` | `[]` | 해당 영역 표시 생략 |
| `keywords` | `[]` (MeSH·KCI 키워드가 **모두** 없을 때 · 1-1) | **해당 키워드 영역 표시 생략 — 카드·상세 공통 (v6.5)** |
| `matchScore` | `null` (빈 검색어 · featured) | 점수 미표시 |
| 검색 결과 | `results: []`, `total: 0` | "검색 결과가 없습니다" |
| featured 결과 | 유효 후보가 3명 미만이면 있는 만큼만 | 받은 개수만큼 표시 |

> `labName` 행은 v6.4에서 삭제되었다 (필드 자체 제거).

**참고 — 데이터가 거의 비어 있는 교수**: 의대 명단에만 있는 교수(주로 기초의학)는 병원 프로필이 없어 `profileImageUrl`·`specialties`가 비어 있다. 논문은 KCI 등 후속 수집으로 보완될 수 있으며, `keywords`는 **MeSH 우선 → MeSH가 없으면 KCI keywords → 둘 다 없으면 `[]`** 규칙으로 채워진다 (v6.5 확정 · 1-1). 따라서 PubMed 논문이 없어 MeSH를 확보하지 못한 교수라도 KCI keywords가 있으면 `keywords`가 비지 않는다. 카드에 이름·구분·소속만 표시되는 경우가 있으므로 프론트는 이 상태에서도 레이아웃이 깨지지 않아야 한다.

---

## 5. 아직 확정이 필요한 항목

1. **`matchScore` 최종 산식** — 계약상 `0~1` 실수(또는 `null`)로만 정의되며 계산 방법은 백엔드/검색엔진팀 소관. 현재 MVP 산식이 동작 중이나 팀 최종 확정은 아니다.
2. **`minScore` 최종 threshold** — 현재 `0.3`은 임시값. 산식이 확정되면 점수 분포에 맞춰 함께 정한다.
   - 실측 사례: `"통증"` 검색 시, 전문분야에 "통증"이 있는 교수가 소속명에 없다는 이유로 0.2점을 받아 임계값 0.3에서 제외됨. 가중치 또는 threshold 조정이 필요하다.
3. **검색어 별칭 규칙 (한↔영 · 학과/진료과)** — 2026-08-16 회의에서 도입은 결정, **구현은 마지막 단계**로 합의.
   - 목적 ①: 한글 검색어를 영문 데이터(논문 제목·초록, 그리고 MeSH를 쓰는 교수의 `keywords`)와 연결 (예: `심장` → `heart`, `cardiac`, `cardiovascular`)
   - 목적 ②: 같은 진료과의 표기 차이를 흡수 (예: `내과학교실(심장)` ↔ `심장내과`)
   - 방식: 별칭표를 미리 만들어 저장소에 두고 검색 시에는 조회만 한다. 표에 없는 검색어는 KMLE 1:1 번역 → AI 보완 순으로 처리한다
   - **이는 `검색어` 쪽에 별칭을 붙이는 규칙이며, `keywords` 값 자체를 번역·변환하는 것이 아니다.** `keywords`(특히 KCI keywords)에는 별도 번역 과정을 두지 않는다는 v6.5 확정 정책과 충돌하지 않는다 (1-1)
   - 미해결: 별칭표 파일 형식·관리 주체, 백엔드 검색 로직 반영 시점

> **MeSH 미확보 교수의 `keywords` 보완 정책은 v6.5에서 확정되어 이 목록에서 내려왔습니다.** 아래 표의 `keywords` 데이터 선택 정책 행과 1-1을 따릅니다.

### 이미 결정되어 이 목록에서 내려온 항목

| 항목 | 결정 |
| --- | --- |
| `keywords` 데이터 선택 정책 (구 "MeSH 미확보 교수의 `keywords` 보완 정책") | **확정 (v6.5)** — 최종 `keywords`는 **① MeSH가 있으면 MeSH → ② MeSH가 없으면 KCI keywords → ③ 둘 다 없으면 `[]`**. 원본은 **`meshTerms` / `kciKeywords`로 출처별 분리 보존**(내부 필드 · 응답에 추가하지 않음)하고, **KCI keywords에 별도 번역(AI·KMLE) 과정을 두지 않는다**. 백엔드는 **최종 `keywords` 전체 배열**을 응답에 담고 **검색 매칭에도 전체 배열**을 사용한다. 화면은 **카드 앞 4개 + `+N`**, **상세 전체**, **`[]`이면 영역 숨김**. v6.4의 "MeSH 기반 영문 키워드"와 KCI 키워드 영문 변환 후보안을 대체한다 (1-1) |
| `keywords` 값의 언어 | **계약 대상 아님 (v6.5)** — MeSH를 쓰면 영문, KCI keyword를 쓰면 KCI 원본 그대로다. **KCI가 제공하는 한글·영문 중 어느 쪽을 우선할지는 이 계약에서 규정하지 않는다** (1-1) |
| `keywords` 화면 노출 | **확정 (v6.5)** — 교수 카드는 앞 4개 + 초과분 `+N`, 교수 상세는 전체 표시, `[]`이면 해당 영역 숨김. 프론트는 **응답 순서를 그대로 사용하고 재정렬하지 않는다**. v6.4의 "미표시"를 대체 (1-1) |
| `keywords` 배열의 내부 정렬 순서 | **프론트 화면 표시 계약의 범위 밖 (v6.5)** — 백엔드/검색엔진팀 내부 사항이다. (출처 선택 규칙은 위 `keywords` 데이터 선택 정책 행으로 확정되었고, 범위 밖인 것은 **배열의 순서**뿐이다.) 프론트는 응답 순서를 그대로 사용하므로 **프론트 구현 방식에는 영향이 없고, `keywords` 관련 프론트 구현을 위해 추가 결정을 기다릴 사항도 없다**. 다만 배열의 순서는 카드에 어떤 `keywords`가 노출되는지를 결정하므로, 순서를 바꾸면 화면에 보이는 앞 4개도 함께 달라진다 |
| 논문 식별자 응답 구조 | **확정 (v6.5)** — `papers[].articleId` **단일 필드(string · 응답 논문 필수)**. `"ART"`로 시작하면 KCI, 아니면 PMID이며 백엔드가 유효성을 보장한다. v6.4의 `pmid` / `kciId` 두 필드를 대체한다 (원칙 1 · 1-2) |
| 논문 원문 링크 URL 조립 | **확정 (v6.5)** — `articleId`의 `"ART"` 접두사로 판별. KCI는 `ciSereArtiView.kci?sereArticleSearchBean.artiId={articleId}`, 그 외는 `pubmed.ncbi.nlm.nih.gov/{articleId}/`. URL 문자열은 저장하지 않고 `articleId`로 조립한다 (3장) |
| 교수 DB 대상 범위 | 확정 (v6.4) — 의대 공식 명단 기준. 치과 계열·병원 전용 교수 제외 (0-2장) |
| 국내 학술지 논문 처리 | 확정 (v6.4) — PubMed에 있으면 PubMed+KCI 둘 다 수집, 없으면 KCI. **응답 표현은 v6.5에서 `articleId` 단일 필드로 통합되었다** (PMID 우선, 없으면 `ART...`) |
| PubMed·KCI 중복 논문 | 확정 (v6.4) — DOI → 제목+연도로 판별, 하나로 병합하고 **내부 데이터에 두 원본 식별자 보존**. **적용 범위는 동일 교수 내부다 (v6.5 명시)** — 응답에는 `articleId` 하나만 나간다 (1-2) |
| 동일 논문의 복수 교수 귀속 | **확정 (v6.5)** — 검증된 공저 논문은 각 교수에게 모두 귀속. **교수 레코드 간 동일 `articleId` 허용**(교수 간 중복 제거 금지), **동일 교수 내부에서는 같은 논문 중복 저장 금지**. 귀속은 해당 교수가 실제 저자임이 확인된 경우에만 인정 (1-2) |
| `labName` | 필드 삭제 (v6.4) — 수집 가능한 출처 없음 |
| 데이터 갱신·배포 구성 | 확정 (v6.4) — 7장 참고 |
| "찜한 교수만 보기" 백엔드 교집합 | 해결 (v6.2) |
| 빈 `query` browse 정책 / `matchScore` null 허용 | 확정 (v6.2) |
| featured 선정 기준·반환 개수 | 확정 (v6.2) — 최근 논문 발행일 최신순 상위 3명 |
| featured 선정용 `latestPaper` 산정 범위 | **확정 (v6.5)** — **PubMed 논문만** 대상. KCI-only 논문(`articleId`가 `"ART"`로 시작)은 제외하며, PubMed 논문이 없는 교수는 featured 후보에서 제외. `papers`의 포함 범위와는 별개이고, **`articleId` 통합으로 바뀌지 않는다**. KCI-only를 빼므로 **KCI·PubMed 발행일 정밀도 차이가 featured에 영향을 주지 않는다** (2장 API ③) |
| `latestPaper` 내부 스키마 | **확정 (v6.5)** — `{ "pmid": string, "publishedAt": string }` 또는 **`null`**. PubMed 논문이 있을 때만 object를 만들고 없으면 `null`, featured 정렬은 `latestPaper.publishedAt` 기준. **내부 필드라 API ③ 응답에 포함하지 않는다.** `publishedAt`의 값 포맷은 아직 확정하지 않았다 (2장 API ③) |
| 검색 대상 필드 | 확정 (v6.2) — 교수명·keywords·specialties·논문 제목·초록·department |
| API ① HTTP 경로·메서드 | 확정 (v6.2) — `POST /api/professors/search` |
| 로그인 기능 | 미구현 결정 — 찜은 localStorage |
| 찜 목록 화면의 데이터 조회 | 확정 (v6.3) — API ① 재사용 |

---

## 6. 화면 ↔ API 매핑

| 화면 | 사용하는 것 |
| --- | --- |
| 1. 메인(교수 검색) | `getProfessors`(검색), `getFeaturedProfessors` |
| 2. 검색 결과 | `getProfessors` → 교수 카드(1-1) |
| 3. 교수 상세 | `getProfessorById` |
| 4. 찜 목록 | localStorage(`getFavorites`) + API ① `getProfessors("", { filters: { favoriteIds } })` |
| 5. 로그인 | MVP 미구현 |

`keywords` 표시 개수는 화면이 아니라 **컴포넌트 기준**이다 — 1·2·4번 화면은 같은 교수 카드 컴포넌트를 쓰므로 모두 앞 4개 + `+N`, 3번 상세만 전체 표시다 (1-1).

---

## 7. 데이터 갱신 · 배포 구성 (v6.4 신설 · 2026-08-16 회의 확정)

계약 데이터가 어떻게 만들어지고 서비스에 닿는지에 대한 합의 사항이다.

```
[데이터 수집 서버(고정 IP)]  주기 실행(cron) → 수집·조립 → professors.json
                              └ 백엔드(FastAPI)가 같은 서버에서 이 파일을 읽어 API 제공
                                        ↑ HTTPS
[Vercel]  프론트엔드 정적 배포 — 학생이 접속
```

- **서비스 런타임에는 외부 API를 호출하지 않는다.** 백엔드는 완성된 `professors.json`만 읽는다. PubMed·OpenAlex·KCI 장애가 검색 기능에 영향을 주지 않는다
- 수집은 별도 주기로 동작한다 (논문·인용수 주 1회 / 교수 명단 월 1회 예정)
- **KCI API 인증키는 IP에 묶여 있어** 수집은 고정 IP 서버에서만 가능하다. Vercel·GitHub Actions는 IP가 유동이라 KCI 호출에 사용할 수 없다
- `professors.json`에는 교수 이메일이 포함되므로 **공개 저장소에 커밋하지 않는다.** 파일은 담당자가 별도 채널로 전달하거나, 수집 서버에서 직접 생성해 사용한다
- `collectedAt`은 이 파일이 만들어진 날짜다 (원칙 4)
